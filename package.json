{
  "name": "armory-git",
  "version": "1.0.0",
  "description": "",
  "main": "shader.js",
  "scripts": {
    "build:module": "esbuild shader.js --minify --format=esm --bundle=false --outfile=dist/shader.js",
    "build": "npm run build:module"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}